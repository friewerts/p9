{
  "name": "test",
  "private": true,
  "version": "0.0.0",
  "description": "> TODO: description",
  "scripts": {
    "start": "rimraf lib && tsc",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "dependencies": {
    "@p9/js-utils": "^0.0.1"
  }
}
